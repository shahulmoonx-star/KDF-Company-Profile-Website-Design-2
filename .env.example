# Base URL of the .NET Web API backend. Server-side only — do not prefix
# with NEXT_PUBLIC_ unless a client-side fetch genuinely needs it.
# See docs/backend-integration-plan.md
API_BASE_URL=https://localhost:5001/api
